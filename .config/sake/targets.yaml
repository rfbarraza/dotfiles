import:
  - targets/wysd.yaml
  
targets:
  all:
    all: true
    tags: []
    servers: []

  docker-hosts:
    all: false
    tags: [docker-host]

  linux-machines:
    all: false
    tags: [manual-linux]
    