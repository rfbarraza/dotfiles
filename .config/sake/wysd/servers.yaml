servers:
  localhost:
    desc: localhost
    host: localhost
    local: true
    tags: [local]

  bouncer:
    desc: bouncer-wysd.lan
    host: 192.168.86.200 # bouncer-wysd.lan
    local: false
    tags: [remote, manual-linux, docker-host] 
    disable_verify_host: true

  media:
    desc: media-wysd.lan
    host: 192.168.86.201 # media-wysd.lan
    local: false
    tags: [remote, manual-linux, docker-host]
    disable_verify_host: true

  hass:
    desc: ha-wysd.lan (v)
    host: 192.168.86.205 # ha-wysd.lan
    local: false
    tags: [remote, auto-linux, virtual, hass, docker-host]
    user: root
    port: 22
    disable_verify_host: true
  
  hass-os:
    desc: ha-wysd.lan
    host: 192.168.86.205 # ha-wysd.lan
    local: false
    tags: [remote, auto-linux, hass]
    user: root
    port: 22222
    shell: sh
    disable_verify_host: true
